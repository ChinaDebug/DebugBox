# 签名配置文件模板
# 1. 复制此文件为 keystore.properties
# 2. 填写你的签名信息
# 3. 将签名文件放入项目目录（建议放在 app/ 目录下）
# 4. keystore.properties 不会被提交到 Git（已添加到 .gitignore）

storeFile=app/my-release-key.jks
storePassword=你的密钥库密码
keyAlias=my-alias
keyPassword=你的密钥密码
