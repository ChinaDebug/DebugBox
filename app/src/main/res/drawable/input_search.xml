<?xml version="1.0" encoding="utf-8"?>
<!-- 搜索框背景 -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 获得焦点 -->
    <item android:state_focused="true">
        <shape>
            <corners android:radius="10mm" />
            <solid android:color="@color/color_dialog_glass_btn_bg" />
            <stroke android:width="2mm" android:color="@android:color/white" />
        </shape>
    </item>
    <!-- 默认 -->
    <item android:state_focused="false">
        <shape>
            <corners android:radius="10mm" />
            <solid android:color="@color/color_dialog_glass_btn_bg" />
            <stroke android:width="1mm" android:color="@color/color_dialog_glass_btn_stroke" />
        </shape>
    </item>
</selector>